name: CD deployment

on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: [self-hosted, student_course]

    steps:
    - uses: actions/checkout@v3
    - name: kubernetes deploy
      run: kubectl run scalerpod --image=${{ secrets.DOCKERHUB_USERNAME }}/test:latest
